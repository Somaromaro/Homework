# Homework
البرنامج يعمل مثل صندوق دائري:
تدخل فيه حروف (واحد واحد)
وتطلعها بنفس الترتيب
وإذا وصل للنهاية يرجع للبداية

شرح بسيط عن الكود:
إنشاء المخزن
typedef struct {
    char b[N];
    int h;
    int t;
    int c;
} X;

b → مكان تخزين الحروف
h → مكان القراءة (head)
t → مكان الكتابة (tail)
c → عدد العناصر داخل المخزن


التهيئة
f0(&x);

تجعل المخزن فاضي وكل المؤشرات تبدأ من ال0


إدخال الاسم
scanf("%s", s);


اضافة النص المطلوب 
strcat(s, "CE-ESY");

ادخال الحروف الى المخزن
for (...) {
    f3(&x, s[i]);
}
كل حرف يدخل للمخزن واذا امتلئ يطبع overflow


char f4(X *x) {
    if (f2(x)) {
        printf("empty\n");
        return '\0';
        تتحقق هل المخزن فارغ تطبع empty اذا فيه بيانات يأخذ الحرف الاول من المكان h ثم تحرك h خطوة للامام ثم تنقص عدد العناصر ثم ترجع الحرف 

اخراج الحروف 
while (!f2(&x)) {
    printf("%c", f4(&x));
}
يخرج حرف حرف وبيطبع بنفس الترتيب

التأكد من المخزن اذا فارغ 
if (f2(&x)) {
    printf("Buffer is now empty.\n");
}

x->t = (x->t + 1) % N;
يدل على ان المخزن دائري اذا وصل للنهاية بيرجع للبداية
